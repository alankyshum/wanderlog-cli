{
  "name": "wanderlog-cli",
  "version": "0.3.4",
  "private": true,
  "type": "module",
  "description": "Wanderlog CLI + Cloudflare Worker calendar feed",
  "bin": {
    "wlog": "./bin/wlog.mjs"
  },
  "scripts": {
    "test": "node --test test/",
    "test:worker": "node --test worker/test/",
    "test:all": "node --test test/ && node --test worker/test/"
  },
  "engines": {
    "node": ">=22"
  }
}
